port: 7880
bind_addresses:
  - "0.0.0.0"
rtc:
  tcp_port: 7881
  udp_port: 7882
  # Default is LAN-friendly. infra/start-infra.mjs writes a runtime config
  # and flips this to true in cloud mode.
  use_external_ip: false
  stun_servers:
    - stun.l.google.com:19302
redis:
  address: redis:6379
keys:
  devkey: secret
